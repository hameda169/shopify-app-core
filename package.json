{
  "name": "shopify-app-core",
  "private": true,
  "version": "0.0.0",
  "description": "Shared core packages for Shopify apps (@hameda169/shopify-core-*)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @hameda169/shopify-core-shared && npm run build -w @hameda169/shopify-core-backend -w @hameda169/shopify-core-frontend -w @hameda169/shopify-core-sdk",
    "check": "npm run check --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "overrides": {
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7"
  },
  "engines": {
    "node": ">=18"
  }
}
